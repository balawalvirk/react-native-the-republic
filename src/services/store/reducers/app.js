import {
   
} from "../actionTypes";


const INITIAL_STATE = {

};

const serviceReducers = (state = INITIAL_STATE, action) => {
    switch (action.type) {
    
        default:
            return state;
    }
};


export default serviceReducers